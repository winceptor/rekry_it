<!-- form-category -->

<div class="form-group">
	<label for="category">Category: <% if (category && category.category && operation=="edit") { %>###donotchange<% } %> </label>
	<select name="category" class="selectpicker textarea form-control" autofocus>
		<option label=" "></option>
		<% for(k in catnames){ %>
			<option value="<%=k %>"<% if (category && k==category.category) { %> selected="selected"<% } %>><%=catnames[k] %></option>
		<% } %>
	</select>
</div>

<div class="form-group">
	<label for="name">###name: </label>
	<input type="text" class="form-control" name="name" value="<% if (category) { %><%= category.name %><% } %>">
</div>
