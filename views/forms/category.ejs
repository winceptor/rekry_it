<!-- form-category -->
<% if (typeof category == "undefined") { category = false; } %>

<div class="form-group">
	<label class="col-sm-2 control-label">###category###: <% if (category && category.category && operation=="edit") { %>###donotchange###<% } %></label>
	<div class="col-sm-8">
		<select name="category" class="selectpicker textarea form-control" title="###select###">
			<% for(k in catnames){ %>
				<option value="<%=k %>"<% if (category && k==category.category) { %> selected="selected"<% } %>><%=catnames[k] %></option>
			<% } %>
		</select>
	</div>
</div>

<div class="form-group">
	<label class="col-sm-2 control-label">###name###: </label>
	<div class="col-sm-8">
		<input type="text" class="form-control" name="name" value="<% if (category) { %><%= category.name %><% } %>" autofocus>
	</div>
</div>
