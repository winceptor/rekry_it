<% layout ('layout') -%>
<div id="content">
    <div class="container">
		
		<% var job = data; %>
		<div class="container shadow">
			<% include ../details/job-extended %>
		</div>
						
		<hr class="empty-divider">
		
		<div class="row">
			<div class="col-sm-12">
				<% if (user) { %>
					<% if (job.user && (user._id==job.user._id || user._id==job.user)) { %>

					<% } else { %>
						<% if (job.displayDate>=Today) { %>
							<a role="button" href="/apply/<%= job._id %>" class="col-xs-4 col-sm-offset-4 btn btn-primary btn-lg btn-spacing">###apply###</a>
						<% } else { %>
							<button disabled class="col-xs-4 col-sm-offset-4 btn btn-primary btn-lg">###closed###</button>
						<% } %>
					<% } %>
					<% if (user.admin) { %>
						<div class="pull-right">
							<a role="button" href="/admin/edit-job/<%= job._id %>" class="btn btn-primary btn-lg btn-warning btn-spacing">###edit###</a>
							<a role="button" href="/admin/delete-job/<%= job._id %>" class="btn btn-primary btn-lg btn-warning btn-spacing">###delete###</a>
						</div>
					<% } %>
				<% } else { %>
					<a role="button" href="/user/signup" class="col-xs-4 col-sm-offset-4 btn btn-primary btn-lg btn-spacing">###signup###</a>
				<% } %>
			</div>
		</div> 	
	</div>
</div>