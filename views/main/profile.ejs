<% layout ('layout') -%>
<div id="content">
    <div class="container">
			
		<% var entry = data; %>
		<div class="container shadow">
			<% include ../details/user %>
		</div>
		
		<hr class="empty-divider">
		
		<div class="row">
			<div class="col-sm-12">
				<% if (user) { %>
					<% if (user.admin) { %>
						<div class="pull-right">
							<a role="button" href="/admin/edit-user/<%= entry._id %>" class="btn btn-primary btn-lg btn-warning">###edit###</a>
							<a role="button" href="/admin/delete-user/<%= entry._id %>" class="btn btn-primary btn-lg btn-warning">###delete###</a>
						</div>
					<% } %>
				<% } %>
				
			</div>
		</div> 	
	</div>
</div>