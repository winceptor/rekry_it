<% layout('layout') -%>
<div id="content">
<% include ../partials/admin-menu %>
<div class="container">
	<h1 class="title">###applications###</h1>

	<% include ../partials/searchbar %>
	
	<div class="container row" id="search">
		<div id="searchresults" class="col-md-12">
			<% for(var i=0; i<data.length;i++){ var favorite = data[i]; %>
				<hr class="empty-divider">
				<%  if (favorite) { %>
					<div class="list-entry">
						<div class="list-entry-head btn-primary" onclick="$( this ).parent().toggleClass( 'expand' );">
							<p><span><%= favorite.user.name %> <strong><%= favorite.job.title %></strong></span><span class="pull-right"><strong>###createdate###: </strong><%= DateToOutput(application.date) %></span></p>
						</div>
						<div class="list-entry-data">
							<% include ../details/favorite %>
						</div>
						<div class="list-entry-buttons">
							<button role="button" onclick="window.open('/favorite/<%= favorite._id %>');" class="btn btn-primary btn-sm" target="_blank">###details###</button>
							<div class="pull-right">
								<button onclick="window.open('/admin/edit-favorite/<%= favorite._id %>');" class="btn btn-sm btn-admin btn-simple">###edit###</button>
								<button onclick="confirm_command('###delete### <%= favorite.title %>','/admin/delete-favorite/<%= favorite._id %>?stay=true');" class="btn btn-sm btn-admin btn-simple">###delete###</button>
							</div>
						</div>
					</div>
				<% } else { %>
					<div class="list-entry">
						<div class="list-entry-head btn-primary" onclick="$( this ).parent().toggleClass( 'expand' );">
							<p><span><strong><%= entry %>:</strong></span><span class="pull-right"><strong>ID: </strong><%= i %></span></p>
						</div>
						<div class="list-entry-data">
							
						</div>
					</div>
				<% } %>
				
			<% } %>
		</div>
	</div>
		
	<% include ../partials/paginator %>
</div>
</div>